CREATE TABLE amarozeroquattro_masteraip ( id uuid, device_id varchar, device_type varchar, system_date timestamp,sample_id varchar, sample_date varchar, out_side_temperature float, max_temperature float, min_temperature float, unit_of_measure varchar, longitudine varchar, latitude varchar, altitude varchar, device_date timestamp, id_tenant int, id_enterprise int,  PRIMARY KEY ((device_id, device_type), system_date)) WITH CLUSTERING ORDER BY (system_date DESC); CREATE INDEX IF NOT EXISTS amarozeroquattro_masteraip_id ON amarozeroquattro_masteraip(id);CREATE INDEX IF NOT EXISTS amarozeroquattro_masteraip_id_enterprise ON amarozeroquattro_masteraip(id_enterprise); CREATE INDEX IF NOT EXISTS amarozeroquattro_masteraip_id_tenant ON amarozeroquattro_masteraip(id_tenant);
